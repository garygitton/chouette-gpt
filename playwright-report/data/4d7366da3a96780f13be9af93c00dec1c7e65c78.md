# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: chat-bdd.spec.ts >> ChouetteGPT - E2E BDD Conversations and Behavior >> Simuler une conversation et manipuler les fonctionnalités principales
- Location: tests/e2e/chat-bdd.spec.ts:11:3

# Error details

```
TimeoutError: page.goto: Timeout 60000ms exceeded.
Call log:
  - navigating to "http://chouette-gpt.localhost/?mock=true", waiting until "domcontentloaded"

```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | test.describe('ChouetteGPT - E2E BDD Conversations and Behavior', () => {
  4   |   
  5   |   test.beforeEach(async ({ page }) => {
  6   |     // Block font files which can hang page.goto and page.screenshot in sandboxed environments
  7   |     await page.route('**/*.{woff,woff2,ttf}', route => route.abort());
  8   |     await page.route('**/*font*', route => route.abort());
  9   |   });
  10  | 
  11  |   test('Simuler une conversation et manipuler les fonctionnalités principales', async ({ page }) => {
  12  |     
  13  |     await test.step('Given je suis sur la page d\'accueil de ChouetteGPT en mode bouchonné', async () => {
  14  |       // open with mock=true to activate Pinia mock engine
> 15  |       await page.goto('/?mock=true', { waitUntil: 'domcontentloaded' });
      |                  ^ TimeoutError: page.goto: Timeout 60000ms exceeded.
  16  |       
  17  |       // Verify app title
  18  |       await expect(page).toHaveTitle(/ChouetteGPT/);
  19  |       
  20  |       // Wait for Nuxt app to mount (wait for sidebar to exist in DOM)
  21  |       await page.waitForSelector('aside', { timeout: 15000 });
  22  |       
  23  |       // Verify sidebar is visible
  24  |       const sidebar = page.locator('aside');
  25  |       await expect(sidebar).toBeVisible();
  26  |       
  27  |       // Verify welcoming title
  28  |       const welcomeHeader = page.locator('h1');
  29  |       await expect(welcomeHeader).toContainText('ChouetteGPT');
  30  |       
  31  |       // Verify prompt suggestions are loaded
  32  |       const suggestionButtons = page.locator('button:has-text("Explique-moi")');
  33  |       await expect(suggestionButtons).toBeVisible();
  34  |     });
  35  | 
  36  |     let conversationUrl: string;
  37  | 
  38  |     await test.step('When je saisis un message et que je l\'envoie', async () => {
  39  |       const textarea = page.getByPlaceholder('Envoyez un message...');
  40  |       await textarea.fill('Explique-moi la théorie de la relativité.');
  41  |       
  42  |       const submitBtn = page.locator('button:has(.pi-send)');
  43  |       await expect(submitBtn).toBeEnabled();
  44  |       await submitBtn.click();
  45  |     });
  46  | 
  47  |     await test.step('Then l\'application affiche l\'état de chargement du modèle', async () => {
  48  |       // Should show the loading overlay
  49  |       const loadingOverlay = page.locator('h2:has-text("Chargement du modèle IA")');
  50  |       await expect(loadingOverlay).toBeVisible();
  51  |       
  52  |       // Wait for model downloading steps to finish (mock takes ~500ms)
  53  |       await expect(loadingOverlay).not.toBeVisible({ timeout: 5000 });
  54  |     });
  55  | 
  56  |     await test.step('And l\'IA locale répond sous forme de flux (streaming)', async () => {
  57  |       // The assistant message bubble should appear and be filled with streamed text
  58  |       const assistantMessage = page.locator('div.pt-1 >> text=ChouetteGPT');
  59  |       await expect(assistantMessage).toBeVisible();
  60  |       
  61  |       // Wait for completion (our mock has a specific text)
  62  |       await expect(assistantMessage).toContainText('IA locale simulée (bouchonnée) pour les tests E2E');
  63  |       await expect(assistantMessage).toContainText('théorie de la relativité');
  64  |       
  65  |       // Keep track of the URL containing the conversation ID
  66  |       await page.waitForURL(/\/\?id=.+/);
  67  |       conversationUrl = page.url();
  68  |     });
  69  | 
  70  |     await test.step('And la conversation est enregistrée dans l\'historique', async () => {
  71  |       const historyItem = page.locator('aside a[href*="?id="]');
  72  |       await expect(historyItem).toHaveCount(1);
  73  |       
  74  |       // The title should match the default new conversation title (represented by first message prompt)
  75  |       await expect(historyItem.first()).toContainText('Nouvelle conversation');
  76  |     });
  77  | 
  78  |     await test.step('When je clique sur "Nouveau chat"', async () => {
  79  |       const newChatBtn = page.getByRole('button', { name: 'Nouveau chat' });
  80  |       await newChatBtn.click();
  81  |       
  82  |       // Verify we returned to the clean welcoming page
  83  |       await expect(page).toHaveURL(/http:\/\/chouette-gpt\.localhost\/\??(mock=true)?$/);
  84  |       const welcomeHeader = page.locator('h1');
  85  |       await expect(welcomeHeader).toContainText('ChouetteGPT');
  86  |     });
  87  | 
  88  |     await test.step('Then je peux créer une deuxième conversation', async () => {
  89  |       const textarea = page.getByPlaceholder('Envoyez un message...');
  90  |       await textarea.fill('Comment faire une tarte aux pommes ?');
  91  |       
  92  |       const submitBtn = page.locator('button:has(.pi-send)');
  93  |       await submitBtn.click();
  94  |       
  95  |       // Model loading overlay will be skipped since engine is already loaded!
  96  |       const assistantMessage = page.locator('div.pt-1 >> text=tarte aux pommes');
  97  |       await expect(assistantMessage).toBeVisible({ timeout: 5000 });
  98  |       
  99  |       // Verify we have 2 history items in sidebar
  100 |       const historyItems = page.locator('aside a[href*="?id="]');
  101 |       await expect(historyItems).toHaveCount(2);
  102 |     });
  103 | 
  104 |     await test.step('When je bascule en mode sombre ou clair', async () => {
  105 |       const htmlElement = page.locator('html');
  106 |       
  107 |       // Get current mode by checking sidebar text or HTML class
  108 |       const themeToggleBtn = page.locator('button:has-text("Mode")');
  109 |       const isInitialDark = await htmlElement.evaluate(el => el.classList.contains('dark'));
  110 |       
  111 |       // Toggle
  112 |       await themeToggleBtn.click();
  113 |       
  114 |       // Verify toggle changed classes
  115 |       if (isInitialDark) {
```